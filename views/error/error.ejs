<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || '404 - Page Not Found' %> | GEARGRID</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Inter', sans-serif;
            background: #000;
            color: #fff;
        }
        
        .error-icon {
            width: 100px;
            height: 100px;
            border: 3px solid #fff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
            animation: pulse 2s ease-in-out infinite;
        }
        
        .error-icon::before {
            content: '!';
            font-size: 60px;
            font-weight: bold;
            color: #fff;
        }
        
        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
                opacity: 1;
            }
            50% {
                transform: scale(1.05);
                opacity: 0.8;
            }
        }
        
        .error-code {
            font-size: 120px;
            font-weight: 900;
            line-height: 1;
            margin-bottom: 1rem;
            letter-spacing: -0.02em;
        }
        
        .btn-primary {
            background: #fff;
            color: #000;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: #e5e5e5;
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(255, 255, 255, 0.1);
        }
        
        .btn-secondary {
            background: transparent;
            color: #fff;
            border: 1px solid #333;
            transition: all 0.3s ease;
        }
        
        .btn-secondary:hover {
            background: #1a1a1a;
            border-color: #555;
            transform: translateY(-2px);
        }
        
        .category-btn {
            background: #1a1a1a;
            border: 1px solid #333;
            transition: all 0.3s ease;
        }
        
        .category-btn:hover {
            background: #2a2a2a;
            border-color: #555;
            transform: translateY(-2px);
        }
        
        .fade-in {
            animation: fadeIn 0.8s ease-in;
        }
        
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body class="min-h-screen flex items-center justify-center p-6">
    <div class="max-w-2xl w-full text-center fade-in">
        <!-- Error Icon -->
        <div class="error-icon"></div>
        
        <!-- Error Code -->
        <h1 class="error-code"><%= errorCode || '404' %></h1>
        
        <!-- Error Title -->
        <h2 class="text-2xl md:text-3xl font-bold mb-4"><%= errorType || 'Page Not Found' %></h2>
        
        <!-- Error Description -->
        <p class="text-gray-400 mb-8 text-base md:text-lg max-w-md mx-auto">
            <%= message || "The page you're looking for doesn't exist or has been moved. Don't worry, let's get you back on track." %>
        </p>
        
        <!-- Primary Actions -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <a href="/" class="btn-primary px-8 py-3 rounded-lg font-semibold inline-flex items-center justify-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path d="M10.707 2.293a1 1 0 00-1.414 0l-7 7a1 1 0 001.414 1.414L4 10.414V17a1 1 0 001 1h2a1 1 0 001-1v-2a1 1 0 011-1h2a1 1 0 011 1v2a1 1 0 001 1h2a1 1 0 001-1v-6.586l.293.293a1 1 0 001.414-1.414l-7-7z" />
                </svg>
                Back to Home
            </a>
        </div>
        
        <!-- Secondary Actions -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <button onclick="searchProducts()" class="btn-secondary px-6 py-2.5 rounded-lg font-medium inline-flex items-center justify-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                </svg>
                Search Products
            </button>
            
            <button onclick="contactSupport()" class="btn-secondary px-6 py-2.5 rounded-lg font-medium inline-flex items-center justify-center gap-2">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                    <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z" />
                </svg>
                Contact Support
            </button>
        </div>
        
        <!-- Divider -->
        <div class="mb-8">
            <p class="text-gray-500 text-sm">Or explore our popular categories:</p>
        </div>
        
        <!-- Category Links -->
        <div class="flex flex-wrap gap-3 justify-center">
            <a href="/keyboards" class="category-btn px-6 py-2.5 rounded-lg text-sm font-medium">
                keyboards
            </a>
            <a href="/mice" class="category-btn px-6 py-2.5 rounded-lg text-sm font-medium">
                mice
            </a>
            <a href="/headset" class="category-btn px-6 py-2.5 rounded-lg text-sm font-medium">
                headset
            </a>
            <a href="/prebuilds" class="category-btn px-6 py-2.5 rounded-lg text-sm font-medium">
                prebuilds
            </a>
        </div>
        
        <!-- Footer Note -->
        <div class="mt-16">
            <p class="text-gray-600 text-xs">
                Error Code: 404 | If this problem persists, please contact our support team
            </p>
        </div>
    </div>

    <script>
        function searchProducts() {
            // Implement search functionality
            window.location.href = '/search';
        }
        
        function contactSupport() {
            // Implement contact support functionality
            window.location.href = '/contact';
        }
        
        // Add smooth scroll behavior
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
    </script>
</body>
</html>