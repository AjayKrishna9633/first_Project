<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GEARGRID - Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/public/css/text-wrap.css">
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
        }
        .glass-card {
            background: rgba(45, 45, 45, 0.95);
            backdrop-filter: blur(10px);
        }
    </style>
</head>
<body class="bg-black min-h-screen flex items-center justify-center p-4">
 
    <div class="w-full max-w-6xl">
        <h1 class="text-white text-4xl font-bold mb-12">GEARGRID</h1>
        
        <div class="glass-card rounded-lg p-12 max-w-md mx-auto">
            <h2 class="text-white text-2xl font-semibold text-center mb-3">LOGIN</h2>
            <p class="text-gray-400 text-center text-sm mb-8">Enter your email and password to login:</p>
            
            <form id="loginPage" action="/login" method="POST" class="space-y-4">
                <!-- Add this AFTER the opening <form> tag or BEFORE the form -->
<% if (typeof message !== 'undefined' && message) { %>
    <div class="<%= isError ? 'bg-red-100 border border-red-400 text-red-700' : 'bg-green-100 border border-green-400 text-green-700' %> px-4 py-3 rounded mb-4">
        <%= message %>
    </div>
<% } %>

                <div>
                    <input 
                        id="email"
                        name="email"
                        placeholder="E-mail" 
                        class="w-full bg-transparent border border-gray-600 rounded px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-gray-400 transition"
                    >
                    <div id="emailError" class="hidden mt-1 text-red-400 text-sm flex items-center gap-1">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                        </svg>
                        <span></span>
                    </div>
                </div>
                
                <div>
                    <div class="relative">
                        <input 
                            id="password"
                            type="password" 
                            name="password"
                            placeholder="Password" 
                            class="w-full bg-transparent border border-gray-600 rounded px-4 py-3 text-white placeholder-gray-500 focus:outline-none focus:border-gray-400 transition"
                        >
                        <a href="/forgot-password" class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 text-xs hover:text-gray-300">
                            Forgot your password?
                        </a>
                    </div>
                    <div id="passwordError" class="hidden mt-1 text-red-400 text-sm flex items-center gap-1">
                        <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                        </svg>
                        <span></span>
                    </div>
                </div>
                <button 
                    type="submit"
                    class="w-full bg-gray-800 hover:bg-gray-700 text-white font-medium py-3 rounded transition mt-6"
                >
                    LOGIN
                </button>
            </form>
            
            <p class="text-gray-400 text-center text-sm mt-6">
                Don't have an account? <a href="/signup" class="text-white hover:underline">Sign up</a>
            </p>
            
            <div class="text-center mt-4">
                <p class="text-gray-500 text-xs mb-3">OR</p>
                <a href="/auth/google" class="bg-white hover:bg-gray-100 text-gray-800 font-medium py-3 px-6 rounded inline-flex items-center gap-2 transition w-full justify-center">
                    <svg class="w-5 h-5" viewBox="0 0 24 24">
                        <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                        <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                    </svg>
                    <span>Login with Google</span>
                </a>
            </div>
        </div>
    </div>
    <script src="/public/user/loginValidation.js"></script>
</body>
</html>
