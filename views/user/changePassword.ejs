<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password | GEARGRID</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/public/css/text-wrap.css">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <!-- Change Password Validation -->
    <script src="/public/user/changePasswordValidation.js?v=2"></script></script>
</head>
<body class="bg-black text-white">
    <%- include('../partials/header') %>

    <div class="bg-gray-100 py-8 mt-20">
        <div class="max-w-7xl mx-auto px-6">
            <h1 class="text-3xl font-bold text-black mb-2">Change Password</h1>
            <div class="flex items-center text-sm text-gray-600">
                <a href="/" class="hover:text-black">GearGrid</a>
                <i class="fas fa-chevron-right mx-2 text-xs"></i>
                <a href="/profile" class="hover:text-black">My Account</a>
                <i class="fas fa-chevron-right mx-2 text-xs"></i>
                <span class="text-black">Change Password</span>
            </div>
        </div>
    </div>

    <div class="max-w-7xl mx-auto px-6 py-12">
        <div class="flex gap-8">
            <!-- Sidebar (same as addressPage.ejs) -->
           
 <%- include('../partials/sidebar', { currentPage: 'password' }) %>

            <!-- Change Password Form -->
            <div class="flex-1 max-w-2xl">
                <div class="bg-gray-900 rounded-lg p-8 border border-gray-800">
                    <h2 class="text-2xl font-bold mb-6">Change Your Password</h2>
                    
                    <form id="changePasswordForm" class="space-y-6">
                        <!-- Current Password -->
                        <div>
                            <label class="block text-sm font-medium mb-2">Current Password *</label>
                            <div class="relative">
                                <input type="password" id="currentPassword" name="currentPassword" 
                                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-cyan-400 text-white">
                                <button type="button" onclick="togglePassword('currentPassword')" 
                                    class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-white">
                                    <i class="fas fa-eye" id="currentPassword-icon"></i>
                                </button>
                            </div>
                            <p id="currentPassword-error" class="text-red-500 text-sm mt-1 hidden"></p>
                        </div>

                        <!-- New Password -->
                        <div>
                            <label class="block text-sm font-medium mb-2">New Password *</label>
                            <div class="relative">
                                <input type="password" id="newPassword" name="newPassword" 
                                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-cyan-400 text-white">
                                <button type="button" onclick="togglePassword('newPassword')" 
                                    class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-white">
                                    <i class="fas fa-eye" id="newPassword-icon"></i>
                                </button>
                            </div>
                            <p class="text-gray-400 text-xs mt-1">Must be at least 8 characters with uppercase, lowercase, and number</p>
                            <p id="newPassword-error" class="text-red-500 text-sm mt-1 hidden"></p>
                        </div>

                        <!-- Confirm New Password -->
                        <div>
                            <label class="block text-sm font-medium mb-2">Confirm New Password *</label>
                            <div class="relative">
                                <input type="password" id="confirmPassword" name="confirmPassword" 
                                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg focus:outline-none focus:border-cyan-400 text-white">
                                <button type="button" onclick="togglePassword('confirmPassword')" 
                                    class="absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-white">
                                    <i class="fas fa-eye" id="confirmPassword-icon"></i>
                                </button>
                            </div>
                            <p id="confirmPassword-error" class="text-red-500 text-sm mt-1 hidden"></p>
                        </div>

                        <!-- Buttons -->
                        <div class="flex gap-4 pt-4">
                            <button type="submit" 
                                class="flex-1 px-6 py-3 bg-white text-black rounded-lg hover:bg-gray-200 transition font-medium">
                                Update Password
                            </button>
                            <a href="/profile" 
                                class="flex-1 px-6 py-3 bg-gray-800 text-white rounded-lg hover:bg-gray-700 transition font-medium text-center">
                                Cancel
                            </a>
                        </div>
                    </form>
                </div>

                <!-- Password Requirements Info -->
                <div class="mt-6 bg-gray-900 rounded-lg p-6 border border-gray-800">
                    <h3 class="font-semibold mb-3 flex items-center gap-2">
                        <i class="fas fa-shield-alt text-cyan-400"></i>
                        Password Requirements
                    </h3>
                    <ul class="space-y-2 text-sm text-gray-400">
                        <li class="flex items-center gap-2">
                            <i class="fas fa-check text-green-500"></i>
                            At least 8 characters long
                        </li>
                        <li class="flex items-center gap-2">
                            <i class="fas fa-check text-green-500"></i>
                            Contains uppercase and lowercase letters
                        </li>
                        <li class="flex items-center gap-2">
                            <i class="fas fa-check text-green-500"></i>
                            Contains at least one number
                        </li>
                        <li class="flex items-center gap-2">
                            <i class="fas fa-check text-green-500"></i>
                            Different from current password
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
