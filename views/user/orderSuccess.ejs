<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Success | GearGrid</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/css/text-wrap.css">
</head>
<body class="bg-gray-900 text-gray-100 font-sans antialiased">
    <%- include('../partials/header') %>

    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8 bg-gray-800 p-8 rounded-2xl shadow-xl border border-gray-700 text-center">
            
            <div class="flex justify-center">
                <div class="rounded-full bg-green-900/50 p-6 border-2 border-green-500 shadow-[0_0_20px_rgba(34,197,94,0.3)]">
                    <i class="fas fa-check text-5xl text-green-500"></i>
                </div>
            </div>

            <div>
                <h2 class="mt-6 text-3xl font-extrabold text-white">
                    Order Placed Successfully!
                </h2>
                <div class="mt-4 text-gray-400">
                    <p>Thank you for your purchase.</p>
                    <p class="mt-2 text-sm">Your order <span class="text-cyan-400 font-mono font-bold">#<%= order.orderNumber %></span> has been confirmed.</p>
                </div>
            </div>

            <div class="mt-8 space-y-4">
                <a href="/orders/<%= order._id %>" 
                   class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-cyan-600 hover:bg-cyan-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-cyan-500 transition-colors">
                    View Order Details
                </a>
                
                <a href="/shop" 
                   class="w-full flex justify-center py-3 px-4 border border-gray-600 rounded-lg shadow-sm text-sm font-medium text-gray-300 bg-gray-700 hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors">
                    Continue Shopping
                </a>
            </div>
            
             <p class="mt-4 text-xs text-gray-500">
                A confirmation email has been sent to your registered email address.
            </p>

        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
