   <!-- Navigation -->
    <nav class="fixed w-full top-0 z-50 bg-black/90 backdrop-blur-sm">
        <div class="max-w-7xl mx-auto px-6 py-5">
            <div class="flex items-center justify-between">
                <div class="text-2xl font-bold tracking-tight">
                    <a href="/">GEARGRID</a>
                </div>
                
                <div class="hidden md:flex items-center space-x-8 text-sm font-medium">
                    <a href="/" class="hover:text-cyan-400 transition">HOME</a>
                    <a href="#" class="hover:text-cyan-400 transition">BEST SELLERS</a>
                    <a href="/shop" class="hover:text-cyan-400 transition">SHOP</a>
                    <a href="#" class="hover:text-cyan-400 transition">CONTACT</a>
                </div>
                
                <div class="flex items-center space-x-6">
                    <button class="hover:text-cyan-400 transition text-xl">♡</button>
                    <button class="hover:text-cyan-400 transition text-xl">🛒</button>
                    
                    <% if (typeof user !== 'undefined' && user) { %>
                        <!-- Logged in user - Show profile icon -->
                        <a href="/profile" class="hover:text-cyan-400 transition">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                            </svg>
                        </a>
                    <% } else { %>
                        <!-- Guest user - Show login/signup -->
                        <a href="/login" class="hover:text-cyan-400 transition text-sm font-medium">Login</a>
                        <a href="/signup" class="hover:text-cyan-400 transition text-sm font-medium">Signup</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>