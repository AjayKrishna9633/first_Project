<!-- Account Sidebar Navigation -->
<div class="w-64 flex-shrink-0">
    <div class="space-y-2">
     <!-- Orders -->
<a href="/orders" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'orders' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-shopping-bag w-5"></i>
    <span>Orders</span>
</a>

<!-- Wishlist -->
<a href="/wishlist" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'wishlist' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-heart w-5"></i>
    <span>Wishlist</span>
</a>

<!-- Address -->
<a href="/address" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'address' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-map-marker-alt w-5"></i>
    <span>Address</span>
</a>

<!-- Password -->
<a href="/change-password" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'password' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-key w-5"></i>
    <span>Password</span>
</a>

<!-- Account Detail -->
<a href="/profile" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'profile' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-user w-5"></i>
    <span>Account Detail</span>
</a>

<!-- Wallet -->
<a href="/wallet" class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= currentPage === 'wallet' ? 'bg-white text-black font-medium' : 'text-white hover:bg-gray-900' %>">
    <i class="fas fa-wallet w-5"></i>
    <span>Wallet</span>
</a>

<!-- Logout -->
<a href="/logout" class="flex items-center gap-3 px-4 py-3 text-white hover:bg-gray-900 rounded-lg transition">
    <i class="fas fa-sign-out-alt w-5"></i>
    <span>Logout</span>
</a>

    </div>
</div>
